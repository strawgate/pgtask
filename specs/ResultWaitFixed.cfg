\* The proposed fix: wait_for_result locks the CHILD row so complete_task
\* on that child blocks until the wait row is committed. Expected to PASS.
SPECIFICATION Spec
CONSTANTS
    SourceLocked = TRUE
    Recheck = FALSE
INVARIANTS
    TypeOK
    NoLostWakeup
