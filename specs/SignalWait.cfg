\* pgtask.wait_for_signal.
\* emit_signal's INSERT into pgtask.signals needs FOR KEY SHARE on the task row
\* to validate its foreign key, and the waiter holds FOR UPDATE on that same
\* row. The two conflict, so the waker blocks and the windows never overlap.
\* Expected to PASS, including NoConcurrentRegistration.
SPECIFICATION Spec
CONSTANTS
    SourceLocked = TRUE
    Recheck = FALSE
INVARIANTS
    TypeOK
    NoLostWakeup
    NoConcurrentRegistration
