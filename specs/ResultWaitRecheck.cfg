\* Does re-reading the source after registering close the window?
\* Expected to FAIL, which rules out the cheap fix.
SPECIFICATION Spec
CONSTANTS
    SourceLocked = FALSE
    Recheck = TRUE
INVARIANTS
    TypeOK
    NoLostWakeup
