\* A wider model. Liveness checking is what blows up here, so this
\* configuration checks safety only; TaskLifecycle.cfg covers the temporal
\* properties at the smaller size.
SPECIFICATION Spec
CONSTANTS
    Tasks = {t1, t2, t3}
    Workers = {w1, w2}
    MaxAttempts = 2
INVARIANTS
    Safety
